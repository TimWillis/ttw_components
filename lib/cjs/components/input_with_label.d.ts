export interface input_with_label_interface {
    html: string;
    for_id: string;
    name: string;
    id?: string;
}
declare const _default: ({ html, for_id, name, id }: input_with_label_interface) => string;
export default _default;
